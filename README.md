# TypeScript Bare bone Boilerplate
This boilerplate has some minimal configuration for a TypeScript project.

The entry file is `src/main.ts`

To run
```shell
yarn start
```

To build to `dist`
```shell
yarn build
```
